pipeline{
  agent any
  stages{
    stage('Source Checkout'){
      steps{
        //checkout github code
        git 'https://github.com/supersourabh/HallsProject.git'
      }
    }
    stage('starting server'){
      steps{
        bat 'npm install && start'
      }
    }
  }
}
